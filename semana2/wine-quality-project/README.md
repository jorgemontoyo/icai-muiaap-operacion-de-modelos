# Wine Quality Project

Proyecto de la práctica 2.2 de Operación de Modelos.

El objetivo es entrenar y evaluar un modelo de clasificación sobre el dataset WineQT utilizando un proyecto reproducible gestionado con `uv`.

## Instalación

Desde la raíz del proyecto:

```bash
uv sync --locked
